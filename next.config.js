const path = require(`path`);

// this is not very important but if you want to configure the sass options then you can do it here
// module.exports = {
//   sassOptions: {
//     includePaths: [path.join(__dirname, "/src/styles")],
//   },
// };
